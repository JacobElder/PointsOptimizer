name: Deal Radar pricing

# Runs price_pending_deals.py without depending on a local machine. The Gmail
# capture half runs as a separate claude.ai scheduled routine. Cash prices come
# from fast-flights (free); SerpApi is a capped fallback (--serpapi-cap). Google
# may block scraping from GitHub runner IPs -- if so, runs fall back to SerpApi
# up to the cap and leave the rest queued.
on:
  # AUTOMATIC PRICING IS OFF (on-demand model, chosen 2026-07-25). The capture
  # routine still logs seats.aero alerts into deal_log.json's `pending`, but they
  # are priced on demand from the Deal Radar page (pages/5_Deal_Radar.py), one
  # lookup per click. This job is kept as a MANUAL escape hatch only --
  # run it from the Actions tab to bulk-price the whole pending queue if ever wanted.
  workflow_dispatch: {}

# Never let two pricing runs race on deal_log.json.
concurrency:
  group: deal-radar
  cancel-in-progress: false

permissions:
  contents: write # needs to commit/push deal_log.json updates

jobs:
  price:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install pricing dependencies
        # Only the pricing runtime deps (requests) -- not the full Streamlit/NumPy
        # app stack, which this job never imports.
        run: pip install -r requirements-pipeline.txt

      - name: Configure git identity for commits made by this workflow
        run: |
          git config user.name "Deal Radar bot"
          git config user.email "deal-radar-bot@users.noreply.github.com"

      - name: Price pending deals
        env:
          SERPAPI_KEY: ${{ secrets.SERPAPI_KEY }}
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
        run: python3 price_pending_deals.py
