# Minimal runtime deps for the Deal Radar pricing job (price_pending_deals.py).
# The pricing path only needs requests + fast-flights -- it never imports streamlit or
# numpy at module level. Kept separate from requirements.txt so the CI job doesn't
# install the whole Streamlit stack.
requests>=2.32,<3
fast-flights==3.1.0
