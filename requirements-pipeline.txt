# Minimal deps for the weekly cash-price source check (cash_price_check.yml):
# just the Google Flights lookup path, without the Streamlit/NumPy app stack.
requests>=2.32,<3
fast-flights==3.1.0
# fast-flights 3.1.0 imports typing_extensions but doesn't declare it.
typing_extensions>=4.12
protobuf>=5.27,<8
